{
  "name": "monet",
  "version": "0.0.1",
  "description": "Rendering system for rendering JSON to Canvas",
  "main": "dev/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel src/index.js",
    "dev": "parcel dev/index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel-bundler": "^1.9.3"
  },
  "devDependencies": {
    "babel-polyfill": "^6.26.0"
  }
}
